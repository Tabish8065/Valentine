import 'package:flutter/material.dart';
import 'day_page.dart';

class TeddyPage extends StatelessWidget {
  const TeddyPage({super.key});

  @override
  Widget build(BuildContext context) => const DayPage(day: 10);
}
