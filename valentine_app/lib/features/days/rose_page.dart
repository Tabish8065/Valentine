import 'package:flutter/material.dart';
import 'day_page.dart';

class RosePage extends StatelessWidget {
  const RosePage({super.key});

  @override
  Widget build(BuildContext context) => const DayPage(day: 7);
}
