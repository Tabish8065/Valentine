import 'package:flutter/material.dart';
import 'day_page.dart';

class HugPage extends StatelessWidget {
  const HugPage({super.key});

  @override
  Widget build(BuildContext context) => const DayPage(day: 12);
}
